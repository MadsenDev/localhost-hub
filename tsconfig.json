{
  // Solution root: no files of its own, so editors and `tsc -b` pick up both
  // projects. src/ is checked by tsconfig.app.json, tooling config by
  // tsconfig.node.json.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
